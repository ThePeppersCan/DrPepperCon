IMPORTANT
=========
1. Replace the website with this build.
2. Run add-quidditch-watchtime.sql in the Supabase SQL Editor.
3. Hard refresh every test browser/account (Ctrl+F5).
4. Each affected account should open Quidditch Mode once. Its first heartbeat
   repairs any legacy username row and links it to the correct auth user.

This build also prevents an older zero-second response from making a visible
watch timer jump backwards while a player is actively watching.
