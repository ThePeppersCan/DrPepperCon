GOLDEN SNITCH — LIGHTWEIGHT 30-SECOND RESTART PATCH

This archive contains ONLY the files changed for the Golden Snitch restart fix.
It does not contain the website assets, so it is small enough to download/upload.

INSTALLATION
1. In Supabase -> SQL Editor, run the complete file:
   quidditch-snitch-authoritative-30-second-fix.sql

2. In your website root, replace these two existing files:
   - script.js
   - index.html

3. Redeploy the site and hard-refresh each test browser (Ctrl+F5).

EXPECTED RESULT
- Catching the Golden Snitch immediately ends the shared live match.
- The full-time/end screen remains for exactly 30 seconds.
- The next match lineup begins after those 30 seconds for everybody.
- The abandoned original match timer no longer controls the restart.

IMPORTANT
The SQL migration and website files are both required. Uploading only script.js
will retain the old authoritative server timer.
